# Production API + WebSocket endpoints (Render-hosted backend).
# Vite loads this automatically during `npm run build`. These are public URLs,
# not secrets. Update them if the backend host/URL changes.
VITE_API_URL=https://hft-arena-backend.onrender.com/api
VITE_WS_URL=wss://hft-arena-backend.onrender.com/ws
